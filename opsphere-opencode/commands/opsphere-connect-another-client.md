---
description: Connect your existing Opsphere account to another MCP client.
---

# Connect another client

Follow the installed `connect-another-client` skill. Use the live `opsphere://clients/connect` resource when available; its generated fallback is `skills/connect-another-client/references/connect.md`. Do not duplicate per-client instructions here. Never copy OAuth tokens between applications.
