{
  "name": "opsphere-cursor-plugin",
  "version": "1.0.17",
  "private": true,
  "description": "Opsphere DevOps & SRE intelligence plugin for Cursor (manifest, rules, skills — no npm runtime deps)",
  "scripts": {
    "validate": "bash scripts/ci-validate.sh && node scripts/phase6-ux-invariants.mjs && node scripts/multiclient-release-invariants.mjs && node --test scripts/warp-package.test.mjs",
    "test": "npm run validate",
    "test:phase6": "node scripts/phase6-ux-invariants.mjs",
    "test:multiclient-release": "node scripts/multiclient-release-invariants.mjs && node --test scripts/warp-package.test.mjs",
    "audit": "node -e \"console.log('No npm dependencies — audit skipped')\""
  }
}
